import { chromium, firefox, webkit, type Browser, type Page } from 'playwright';
import { createHash } from 'node:crypto';
import type { BrowserFact, BrowserType, PerformanceSnapshot, Viewport } from '@visionqa/contracts';
import type { BrowserScanJob } from '@visionqa/queue';
import { queueNames } from '@visionqa/queue';
import { Worker, type Job } from 'bullmq';
import { FirebaseBrowserExecutionRepository, FirebaseEvidenceRepository, FirebaseEvidenceStorage, FirebaseIssueRepository, FirebaseScanRepository } from '@visionqa/database/firebase';
import { NetworkPolicyError, OutboundNetworkPolicy } from '@visionqa/network-policy';
import { analyzeAccessibilitySeo, analyzePerformance, classifyInteraction, compareVisualViewports, detectVisual, type AccessibilityElement, type InteractionCandidate, type InteractionKind, type PageMetadataSnapshot } from '@visionqa/detectors';
import { renderVisualAnnotation } from '@visionqa/evidence';
import type { VisualFinding, VisualPageSnapshot } from '@visionqa/contracts';

const maxMessages = 200;
const maxMessageLength = 2000;
const profiles: Record<string, Viewport> = { mobile: { width: 390, height: 844 }, tablet: { width: 768, height: 1024 }, desktop: { width: 1366, height: 768 }, wide: { width: 1920, height: 1080 } };
const truncate = (value: string) => value.slice(0, maxMessageLength);
const browserFor = (type: BrowserType) => type === 'firefox' ? firefox : type === 'webkit' ? webkit : chromium;
export function validateBrowserJob(job: BrowserScanJob): void { if (job.capability !== 'browser' || !job.scanId || !job.projectId || !job.viewports.length) throw new Error('Invalid browser job.'); }

export class BrowserWorker {
  private readonly scans = new FirebaseScanRepository();
  private readonly executions = new FirebaseBrowserExecutionRepository();
  private readonly evidence = new FirebaseEvidenceStorage();
  private readonly evidenceRecords = new FirebaseEvidenceRepository();
  private readonly issues = new FirebaseIssueRepository();
  private readonly visualResults = new Map<string, Array<{ snapshot: VisualPageSnapshot; findings: ReturnType<typeof detectVisual> }>>();
  private readonly visualIssueIds = new Map<string, string>();
  async start(): Promise<void> { const connection = { url: process.env.REDIS_URL ?? 'redis://127.0.0.1:6379' }; new Worker<BrowserScanJob>(queueNames.browser, (job) => this.process(job), { connection, concurrency: Number(process.env.BROWSER_CONCURRENCY ?? 1) }); console.log('VisionQA browser worker ready'); }
  validate(job: BrowserScanJob): void { validateBrowserJob(job); }
  private viewport(input: Viewport): Viewport { return input.width && input.height ? input : profiles.desktop!; }
  async process(job: Job<BrowserScanJob>): Promise<void> {
    this.validate(job.data); if (await this.scans.isCancellationRequested(job.data.scanId)) return;
    const scan = await this.scans.findByIdForWorker(job.data.scanId); const targetUrl = scan?.target?.normalizedUrl; if (!targetUrl) throw new Error('Scan has no persisted target URL.'); const effectiveJob = { ...job.data, targetUrl }; const policy = new OutboundNetworkPolicy(); await policy.validateAndResolve(targetUrl); const browserType: BrowserType = job.data.browsers[0] ?? 'chromium'; let browser: Browser | undefined;
    try { browser = await browserFor(browserType).launch({ headless: true }); let completed = 0;
      for (const requestedViewport of job.data.viewports) {
        if (await this.scans.isCancellationRequested(job.data.scanId)) return; const viewport = this.viewport(requestedViewport); const context = await browser.newContext({ viewport: { width: viewport.width, height: viewport.height }, ...(viewport.deviceScaleFactor ? { deviceScaleFactor: viewport.deviceScaleFactor } : {}) }); const execution = await this.executions.create({ scanId: job.data.scanId, projectId: job.data.projectId, pageUrl: targetUrl, browser: browserType, viewport, status: 'RUNNING', consoleErrorCount: 0, pageErrorCount: 0, failedRequestCount: 0 }); let page: Page | undefined; let collecting = true; let consoleErrors = 0; let pageErrors = 0; let failedRequests = 0; const started = Date.now(); const facts: Array<Omit<BrowserFact, 'id' | 'timestamp'>> = []; let performanceSnapshot: PerformanceSnapshot | undefined;
        try {
          await context.route('**/*', async (route) => { const url = route.request().url(); if (url === 'about:blank') return route.continue(); try { const parsed = new URL(url); if (!['http:', 'https:'].includes(parsed.protocol)) throw new NetworkPolicyError('Unsupported browser request protocol'); await policy.validateAndResolve(url); await route.continue(); } catch (error) { if (collecting) facts.push({ scanId: job.data.scanId, executionId: execution.id, kind: 'NETWORK_POLICY_BLOCKED', url, resourceType: route.request().resourceType(), message: error instanceof Error ? truncate(error.message) : 'Request blocked by network policy' }); await route.abort('blockedbyclient'); } });
          page = await context.newPage(); page.on('console', (message) => { if (collecting && message.type() === 'error' && consoleErrors < maxMessages) { consoleErrors++; facts.push({ scanId: job.data.scanId, executionId: execution.id, kind: 'CONSOLE', type: message.type(), message: truncate(message.text()), source: message.location().url }); } }); page.on('pageerror', (error) => { if (collecting && pageErrors < maxMessages) { pageErrors++; facts.push({ scanId: job.data.scanId, executionId: execution.id, kind: 'PAGE_ERROR', message: truncate(error.message), source: truncate(error.stack ?? '') }); } }); page.on('request', (request) => { if (collecting && facts.length < maxMessages * 4) facts.push({ scanId: job.data.scanId, executionId: execution.id, kind: 'REQUEST', url: request.url(), type: request.method(), resourceType: request.resourceType() }); }); page.on('response', (response) => { if (collecting && facts.length < maxMessages * 4) facts.push({ scanId: job.data.scanId, executionId: execution.id, kind: 'RESPONSE', url: response.url(), status: response.status(), resourceType: response.request().resourceType() }); }); page.on('requestfailed', (request) => { if (collecting && failedRequests < maxMessages) { failedRequests++; facts.push({ scanId: job.data.scanId, executionId: execution.id, kind: 'FAILED_REQUEST', url: request.url(), resourceType: request.resourceType(), message: truncate(request.failure()?.errorText ?? 'Request failed') }); } });
          const response = await page.goto(targetUrl, { waitUntil: 'domcontentloaded', timeout: Number(process.env.BROWSER_NAVIGATION_TIMEOUT_MS ?? 30000) }); if (await this.scans.isCancellationRequested(job.data.scanId)) return; if (job.data.module === 'visual-responsive') await this.persistVisualFindings(effectiveJob, await this.snapshot(page, job.data.scanId, execution.id, viewport)); if (job.data.module === 'interactions-forms') await this.runInteractions(effectiveJob, page, execution.id, viewport); if (job.data.module === 'accessibility-seo') await this.runAccessibility(effectiveJob, page, execution.id, viewport); if (job.data.module === 'performance-compatibility') performanceSnapshot = await this.runPerformance(effectiveJob, page, execution.id, viewport, failedRequests); await page.goto(targetUrl, { waitUntil: 'domcontentloaded', timeout: Number(process.env.BROWSER_NAVIGATION_TIMEOUT_MS ?? 30000) }); const screenshot = await page.screenshot({ fullPage: true }); if (await this.scans.isCancellationRequested(job.data.scanId)) return; const storageKey = `projects/${job.data.projectId}/scans/${job.data.scanId}/browser/${execution.id}/${viewport.width}x${viewport.height}/screenshot.png`; await this.evidence.putObject(storageKey, { contentType: 'image/png', data: screenshot }); const evidence = await this.evidenceRecords.create({ projectId: job.data.projectId, scanId: job.data.scanId, executionId: execution.id, type: 'SCREENSHOT', storagePath: storageKey, contentType: 'image/png', sizeBytes: screenshot.byteLength, browser: browserType, viewport, pageUrl: page.url() }); if (await this.scans.isCancellationRequested(job.data.scanId)) return; if (job.data.module === 'visual-responsive') { const visualResult = this.visualResults.get(job.data.scanId)?.find((result) => result.snapshot.executionId === execution.id); if (visualResult) await this.createVisualAnnotations(effectiveJob, visualResult.snapshot, visualResult.findings, screenshot, evidence.id); } await this.executions.markCompleted(job.data.scanId, execution.id, { status: 'COMPLETED', finalUrl: page.url(), ...(response?.status() !== undefined ? { httpStatus: response.status() } : {}), durationMs: Date.now() - started, consoleErrorCount: consoleErrors, pageErrorCount: pageErrors, failedRequestCount: failedRequests, screenshotEvidenceId: evidence.id, ...(performanceSnapshot ? { performance: performanceSnapshot } : {}) });
        } catch { if (await this.scans.isCancellationRequested(job.data.scanId)) return; await this.executions.markCompleted(job.data.scanId, execution.id, { status: 'FAILED', durationMs: Date.now() - started, consoleErrorCount: consoleErrors, pageErrorCount: pageErrors, failedRequestCount: failedRequests }); } finally { collecting = false; await page?.close().catch(() => undefined); await context.close().catch(() => undefined); }
        completed++; if (await this.scans.isCancellationRequested(job.data.scanId)) return; await this.executions.addFacts(facts); await this.persistFindings(effectiveJob, facts, viewport); await this.scans.updateProgress(job.data.scanId, { completed, total: job.data.viewports.length, percent: Math.round(completed / job.data.viewports.length * 100), processed: completed, pending: job.data.viewports.length - completed });
      } if (await this.scans.isCancellationRequested(job.data.scanId)) return; await this.scans.complete(job.data.scanId, { pagesChecked: completed, browser: browserType }); this.visualResults.delete(job.data.scanId); this.visualIssueIds.clear();
    } finally { await browser?.close().catch(() => undefined); }
  }
  private async persistFindings(job: BrowserScanJob, facts: Array<Omit<BrowserFact, 'id' | 'timestamp'>>, viewport: Viewport): Promise<void> { const enabled = new Set(job.checks); const candidates = facts.filter((fact) => (enabled.has('console-errors') && fact.kind === 'CONSOLE' && fact.type === 'error') || (enabled.has('javascript-errors') && fact.kind === 'PAGE_ERROR') || (enabled.has('failed-browser-requests') && fact.kind === 'FAILED_REQUEST') || (enabled.has('http-browser-errors') && fact.kind === 'RESPONSE' && (fact.status ?? 0) >= 400)); for (const fact of candidates.slice(0, maxMessages * 4)) { if (await this.scans.isCancellationRequested(job.scanId)) return; const detectorId = fact.kind === 'CONSOLE' ? 'console-errors' : fact.kind === 'PAGE_ERROR' ? 'javascript-errors' : fact.kind === 'FAILED_REQUEST' ? 'failed-browser-requests' : 'http-browser-errors'; const pageUrl = fact.url ?? job.targetUrl; await this.issues.upsertFinding({ projectId: job.projectId, detectorId, module: 'browser-network', severity: detectorId === 'javascript-errors' ? 'medium' : 'low', title: detectorId === 'http-browser-errors' ? 'Browser HTTP error' : detectorId === 'failed-browser-requests' ? 'Browser request failed' : detectorId === 'javascript-errors' ? 'JavaScript error on page' : 'Console error on page', message: truncate(fact.message ?? `Browser response returned HTTP ${fact.status ?? 'error'}.`), fingerprint: `${detectorId}|${pageUrl}|${fact.message ?? fact.status ?? ''}`, primaryUrl: pageUrl, scanId: job.scanId, evidence: { browser: job.browsers[0] ?? 'chromium', viewport, factKind: fact.kind, resourceUrl: fact.url, status: fact.status } }); } }
  private async createVisualAnnotations(job: BrowserScanJob, snapshot: VisualPageSnapshot, findings: VisualFinding[], screenshot: Buffer, sourceScreenshotEvidenceId: string): Promise<void> { for (const finding of findings.slice(0, 50)) { if (finding.detectorId === 'responsive-layout' || await this.scans.isCancellationRequested(job.scanId)) return; try { const rendered = await renderVisualAnnotation(screenshot, finding.elements.map((element) => ({ ref: element.ref, ...element.rect }))); if (!rendered.rectangles.length) continue; if (await this.scans.isCancellationRequested(job.scanId)) return; const hash = createHash('sha256').update(`${snapshot.executionId}|${finding.fingerprint}`).digest('hex').slice(0, 40); const storagePath = `projects/${job.projectId}/scans/${job.scanId}/visual/${snapshot.executionId}/${hash}/annotation.png`; await this.evidence.putObject(storagePath, { contentType: rendered.contentType, data: rendered.image }); const annotation = await this.evidenceRecords.create({ projectId: job.projectId, scanId: job.scanId, executionId: snapshot.executionId, type: 'VISUAL_ANNOTATION', storagePath, contentType: rendered.contentType, sizeBytes: rendered.image.byteLength, browser: snapshot.browser, viewport: snapshot.viewport, pageUrl: snapshot.pageUrl, metadata: { detectorId: finding.detectorId, findingFingerprint: finding.fingerprint, sourceScreenshotEvidenceId, elementRefs: finding.elements.map((element) => element.ref).join(','), rectangles: JSON.stringify(rendered.rectangles), annotationStatus: 'AVAILABLE' } }); const issueId = this.visualIssueIds.get(`${job.scanId}|${finding.fingerprint}`); if (issueId) await this.issues.attachOccurrenceEvidence(job.projectId, issueId, job.scanId, { originalScreenshotEvidenceId: sourceScreenshotEvidenceId, annotationEvidenceId: annotation.id }); } catch (error) { console.error(JSON.stringify({ event: 'ANNOTATION_FAILED', scanId: job.scanId, executionId: snapshot.executionId, detectorId: finding.detectorId, error: error instanceof Error ? error.message : 'annotation failed' })); } } }
  private async snapshot(page: Page, scanId: string, executionId: string, viewport: Viewport): Promise<VisualPageSnapshot> { return page.evaluate(({ scanId: currentScanId, executionId: currentExecutionId, viewport: currentViewport }) => { const excluded = new Set(['SCRIPT', 'STYLE', 'META', 'LINK', 'HEAD', 'NOSCRIPT']); const nodes = Array.from(document.querySelectorAll('*')).filter((node) => !excluded.has(node.tagName)).slice(0, 1000); const refs = new Map<Element, string>(nodes.map((node, index) => [node, `e${index}`])); const elements = nodes.map((node) => { const element = node as HTMLElement; const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); const selector = element.id ? `#${CSS.escape(element.id)}` : element.tagName.toLowerCase(); const text = (element.innerText ?? '').trim().replace(/\s+/g, ' ').slice(0, 160); const points = [{ x: rect.left + Math.min(rect.width / 2, 8), y: rect.top + Math.min(rect.height / 2, 8) }, { x: rect.right - Math.min(rect.width / 2, 8), y: rect.bottom - Math.min(rect.height / 2, 8) }]; const paintedAboveRefs = style.position === 'fixed' || style.position === 'sticky' ? points.flatMap((point) => document.elementsFromPoint(point.x, point.y)).map((item) => refs.get(item)).filter((ref): ref is string => Boolean(ref) && ref !== refs.get(element)).slice(0, 20) : []; return { ref: refs.get(element)!, tagName: element.tagName.toLowerCase(), textPreview: text, ...(element.getAttribute('role') ? { role: element.getAttribute('role')! } : {}), selector, rect: { x: rect.x, y: rect.y, width: rect.width, height: rect.height }, display: style.display, visibility: style.visibility, position: style.position, overflow: style.overflow, overflowX: style.overflowX, overflowY: style.overflowY, zIndex: style.zIndex, fontSize: style.fontSize, lineHeight: style.lineHeight, whiteSpace: style.whiteSpace, interactive: /^(A|BUTTON|INPUT|SELECT|TEXTAREA|IMG|VIDEO|IFRAME)$/.test(element.tagName) || Boolean(element.getAttribute('role')), clientWidth: element.clientWidth, clientHeight: element.clientHeight, scrollWidth: element.scrollWidth, scrollHeight: element.scrollHeight, lineClamp: style.webkitLineClamp, textOverflow: style.textOverflow, ...(paintedAboveRefs.length ? { paintedAboveRefs } : {}) }; }).filter((element) => element.visibility !== 'hidden' && element.display !== 'none' && element.rect.width > 0 && element.rect.height > 0); return { scanId: currentScanId, executionId: currentExecutionId, pageUrl: location.href, browser: 'chromium', viewport: currentViewport, documentWidth: Math.max(document.documentElement.scrollWidth, document.body?.scrollWidth ?? 0), documentHeight: Math.max(document.documentElement.scrollHeight, document.body?.scrollHeight ?? 0), elements, capturedAt: new Date().toISOString() }; }, { scanId, executionId, viewport }) as Promise<VisualPageSnapshot>; }
  private async persistVisualFindings(job: BrowserScanJob, snapshot: VisualPageSnapshot): Promise<void> { const selectedSignals = job.checks.filter((check) => check !== 'responsive-layout'); const findings = detectVisual(snapshot, selectedSignals.length ? selectedSignals : ['text-overlap', 'element-overlap', 'clipped-content', 'horizontal-overflow', 'viewport-overflow', 'fixed-element-obstruction']); const results = this.visualResults.get(job.scanId) ?? []; results.push({ snapshot, findings }); this.visualResults.set(job.scanId, results); const output = [...(selectedSignals.length ? findings : []), ...(job.checks.includes('responsive-layout') && results.length === job.viewports.length ? compareVisualViewports(results) : [])]; for (const finding of output) { if (await this.scans.isCancellationRequested(job.scanId)) return; const issue = await this.issues.upsertFinding({ projectId: job.projectId, detectorId: finding.detectorId, module: 'visual-responsive', severity: finding.severity, title: finding.title, message: finding.message, fingerprint: finding.fingerprint, primaryUrl: finding.pageUrl, scanId: job.scanId, evidence: { executionId: snapshot.executionId, browser: snapshot.browser, viewport: snapshot.viewport, elements: finding.elements, ...(finding.detectorId === 'responsive-layout' ? { comparedViewports: results.map((result) => result.snapshot.viewport) } : {}) } }); this.visualIssueIds.set(`${job.scanId}|${finding.fingerprint}`, issue.id); } }

  private async runInteractions(job: BrowserScanJob, page: Page, executionId: string, viewport: Viewport): Promise<void> {
    const candidates = await page.evaluate(() => Array.from(document.querySelectorAll('button, a[href], input[type="button"], input[type="submit"], input[type="reset"], [role="button"], [role="tab"], [aria-haspopup], summary')).slice(0, 50).map((node, index) => { const element = node as HTMLElement; const ref = 'interaction-' + index; element.setAttribute('data-visionqa-interaction-ref', ref); const rect = element.getBoundingClientRect(); const style = getComputedStyle(element); const role = element.getAttribute('role') ?? undefined; const type = element.getAttribute('type') ?? undefined; const textPreview = (element.innerText || element.getAttribute('value') || '').trim().replace(/\s+/g, ' ').slice(0, 160); const accessibleName = (element.getAttribute('aria-label') || element.getAttribute('title') || '').trim().slice(0, 160); const href = element.getAttribute('href') ?? undefined; const kind = role === 'tab' ? 'tabs' : element.tagName === 'SUMMARY' ? 'accordions' : element.tagName === 'A' && (href === '' || href === '#' || role === 'button') ? 'links-as-controls' : element.getAttribute('aria-haspopup') ? 'menus' : 'buttons'; const point = document.elementFromPoint(rect.left + rect.width / 2, rect.top + rect.height / 2); return { ref, selector: '[data-visionqa-interaction-ref="' + ref + '"]', tagName: element.tagName.toLowerCase(), ...(role ? { role } : {}), ...(type ? { type } : {}), textPreview, accessibleName, ...(href ? { href } : {}), disabled: 'disabled' in element && Boolean((element as HTMLInputElement).disabled), ariaDisabled: element.getAttribute('aria-disabled') === 'true', ...(element.getAttribute('aria-expanded') ? { ariaExpanded: element.getAttribute('aria-expanded')! } : {}), ...(element.getAttribute('aria-selected') ? { ariaSelected: element.getAttribute('aria-selected')! } : {}), ...(element.getAttribute('aria-controls') ? { ariaControls: element.getAttribute('aria-controls')! } : {}), visible: style.display !== 'none' && style.visibility !== 'hidden' && rect.width > 0 && rect.height > 0, covered: point !== element && !element.contains(point), rect: { x: rect.x, y: rect.y, width: rect.width, height: rect.height }, kind }; })) as Array<InteractionCandidate & { kind: InteractionKind }>;
    if (job.checks.includes('basic-form-validation')) await this.runFormValidation(job, page, executionId, viewport);
    for (const candidate of candidates.slice(0, 20)) {
      if (await this.scans.isCancellationRequested(job.scanId)) return;
      const safety = classifyInteraction(candidate); const safeCandidate = { ...candidate, safety }; if (!job.checks.includes(candidate.kind)) continue;
      if (!candidate.visible || candidate.disabled || candidate.ariaDisabled || safety === 'UNKNOWN' || safety === 'POTENTIALLY_DESTRUCTIVE' || safety === 'DESTRUCTIVE') continue;
      if (candidate.covered && job.checks.includes('covered-controls')) { await this.recordInteractionFinding(job, executionId, safeCandidate, viewport, 'CONTROL_OBSTRUCTED', 'The control is covered by another element and cannot be safely activated.'); continue; }
      const beforeUrl = page.url(); const before = await page.screenshot({ fullPage: false }).catch(() => undefined); const started = Date.now(); let failure: string | undefined;
      try { await page.locator(candidate.selector).click({ timeout: 3000 }); } catch (error) { failure = error instanceof Error ? error.message.slice(0, 240) : 'Interaction failed'; }
      const after = await page.screenshot({ fullPage: false }).catch(() => undefined); const afterState = await page.evaluate((selector) => { const element = document.querySelector(selector) as HTMLElement | null; return { url: location.href, expanded: element?.getAttribute('aria-expanded') ?? null, selected: element?.getAttribute('aria-selected') ?? null, dialogs: document.querySelectorAll('[role="dialog"], dialog').length }; }, candidate.selector).catch(() => ({ url: page.url(), expanded: null, selected: null, dialogs: 0 })); const changed = beforeUrl !== afterState.url || afterState.expanded !== (candidate.ariaExpanded ?? null) || afterState.selected !== (candidate.ariaSelected ?? null) || afterState.dialogs > 0;
      if (failure || !changed) await this.recordInteractionFinding(job, executionId, safeCandidate, viewport, failure ? 'INTERACTION_FAILED' : 'CONTROL_NO_OP', failure ?? 'The control was activated but no observable outcome was detected.', before, after, Date.now() - started, afterState);
      await page.goto(job.targetUrl, { waitUntil: 'domcontentloaded', timeout: Number(process.env.BROWSER_NAVIGATION_TIMEOUT_MS ?? 30000) }).catch(() => undefined);
    }
  }
  private async runFormValidation(job: BrowserScanJob, page: Page, executionId: string, viewport: Viewport): Promise<void> {
    const invalidForms = await page.evaluate(() => Array.from(document.querySelectorAll('form')).slice(0, 20).flatMap((form, index) => { const requiredInvalid = Array.from(form.querySelectorAll('[required]')).some((field) => !(field as HTMLInputElement).checkValidity()); const emailInvalid = Array.from(form.querySelectorAll('input[type="email"]')).some((field) => { const input = field as HTMLInputElement; const original = input.value; input.value = 'not-an-email'; const invalid = !input.checkValidity(); input.value = original; return invalid; }); return requiredInvalid || emailInvalid ? [{ index, requiredInvalid, emailInvalid }] : []; }));
    for (const form of invalidForms) { const candidate: InteractionCandidate = { ref: `form-${form.index}`, selector: `form:nth-of-type(${form.index + 1})`, tagName: 'form', textPreview: 'Form validation', accessibleName: '', disabled: false, ariaDisabled: false, visible: true, covered: false, rect: { x: 0, y: 0, width: 0, height: 0 }, safety: 'SAFE', kind: 'basic-form-validation' }; await this.recordInteractionFinding(job, executionId, candidate, viewport, 'FORM_VALIDATION', form.requiredInvalid ? 'A required field does not expose usable browser validation.' : 'An email field accepts an invalid value without browser validation.'); }
  }
  private async recordInteractionFinding(job: BrowserScanJob, executionId: string, candidate: InteractionCandidate, viewport: Viewport, detectorId: string, message: string, before?: Buffer, after?: Buffer, durationMs = 0, afterState?: Record<string, unknown>): Promise<void> {
    const evidence: Record<string, unknown> = { executionId, browser: job.browsers[0] ?? 'chromium', viewport, elementRef: candidate.ref, selector: candidate.selector, interactionType: candidate.kind, safetyClassification: candidate.safety, beforeState: { url: job.targetUrl, visible: candidate.visible, disabled: candidate.disabled }, afterState: afterState ?? {}, durationMs, errorCode: detectorId };
    const evidenceIds: Record<string, string> = {};
    for (const [label, data] of [['before', before], ['after', after]] as const) { if (!data) continue; const path = 'projects/' + job.projectId + '/scans/' + job.scanId + '/interactions/' + executionId + '/' + candidate.ref + '/' + label + '.png'; await this.evidence.putObject(path, { contentType: 'image/png', data }); const item = await this.evidenceRecords.create({ projectId: job.projectId, scanId: job.scanId, executionId, type: 'SCREENSHOT', storagePath: path, contentType: 'image/png', sizeBytes: data.byteLength, browser: job.browsers[0] ?? 'chromium', viewport, pageUrl: job.targetUrl, metadata: { interactionType: candidate.kind, elementRef: candidate.ref, evidenceRole: label } }); evidenceIds[label + 'EvidenceId'] = item.id; }
    const issue = await this.issues.upsertFinding({ projectId: job.projectId, detectorId, module: 'interactions-forms', severity: detectorId === 'CONTROL_OBSTRUCTED' ? 'high' : 'medium', title: detectorId === 'CONTROL_OBSTRUCTED' ? 'Interactive control is obstructed' : detectorId === 'CONTROL_NO_OP' ? 'Interactive control may not work' : 'Interactive control failed', message, fingerprint: detectorId + '|' + job.targetUrl + '|' + candidate.ref, primaryUrl: job.targetUrl, scanId: job.scanId, evidence: { ...evidence, ...evidenceIds } }); await this.issues.attachOccurrenceEvidence(job.projectId, issue.id, job.scanId, evidenceIds);
  }
  private async runPerformance(job: BrowserScanJob, page: Page, executionId: string, viewport: Viewport, failedRequestCount: number): Promise<PerformanceSnapshot> {
    const snapshot = await page.evaluate(({ browser, viewport: currentViewport, failed }) => { const navigation = performance.getEntriesByType('navigation')[0] as PerformanceNavigationTiming | undefined; const resources = performance.getEntriesByType('resource').slice(0, 200) as PerformanceResourceTiming[]; const paint = performance.getEntriesByName('first-contentful-paint')[0] as PerformancePaintTiming | undefined; const lcp = performance.getEntriesByType('largest-contentful-paint').at(-1) as PerformanceEntry | undefined; const shifts = performance.getEntriesByType('layout-shift') as Array<PerformanceEntry & { value: number; hadRecentInput?: boolean }>; return { pageUrl: location.href, browser, viewport: currentViewport, navigation: { ttfbMs: navigation ? Math.max(0, navigation.responseStart - navigation.requestStart) : null, domContentLoadedMs: navigation ? Math.max(0, navigation.domContentLoadedEventEnd - navigation.startTime) : null, loadMs: navigation?.loadEventEnd ? Math.max(0, navigation.loadEventEnd - navigation.startTime) : null }, webVitals: { fcpMs: paint?.startTime ?? null, lcpMs: lcp?.startTime ?? null, cls: shifts.length ? shifts.filter((entry) => !entry.hadRecentInput).reduce((total, entry) => total + entry.value, 0) : null }, network: { requestCount: resources.length, failedRequestCount: failed, transferredBytes: resources.length ? resources.reduce((total, entry) => total + (entry.transferSize || 0), 0) : null, encodedBytes: resources.length ? resources.reduce((total, entry) => total + (entry.encodedBodySize || 0), 0) : null }, resources: resources.map((entry) => ({ url: entry.name.slice(0, 500), type: entry.initiatorType || 'other', durationMs: Number.isFinite(entry.duration) ? entry.duration : null, transferSize: entry.transferSize || null, encodedBodySize: entry.encodedBodySize || null, status: null, initiatorType: entry.initiatorType || null, renderBlocking: entry.initiatorType === 'link' || entry.initiatorType === 'script' })) }; }, { browser: job.browsers[0] ?? 'chromium', viewport, failed: failedRequestCount }) as unknown as PerformanceSnapshot;
    for (const finding of analyzePerformance(snapshot, job.checks)) { if (await this.scans.isCancellationRequested(job.scanId)) return snapshot; await this.issues.upsertFinding({ projectId: job.projectId, detectorId: finding.detectorId, module: 'performance-compatibility', severity: finding.severity, title: finding.title, message: finding.message, fingerprint: `${finding.detectorId}|${job.targetUrl}|${String(finding.metadata?.resourceUrl ?? '')}`, primaryUrl: job.targetUrl, scanId: job.scanId, evidence: { executionId, browser: snapshot.browser, viewport, ...(finding.metadata ?? {}) } }); }
    return snapshot;
  }
  private async runAccessibility(job: BrowserScanJob, page: Page, executionId: string, viewport: Viewport): Promise<void> {
    if (await this.scans.isCancellationRequested(job.scanId)) return;
    const snapshot = await page.evaluate(() => { const nodes = Array.from(document.querySelectorAll('button, a, input, select, textarea, img, [role], h1, h2, h3, h4, h5, h6, [tabindex]')).slice(0, 1000); const refs = new Map<Element, string>(nodes.map((node, index) => [node, 'a' + index])); const text = (element: Element) => (element.textContent ?? '').trim().replace(/\s+/g, ' ').slice(0, 160); const name = (element: Element) => { const labelled = element.getAttribute('aria-label') || element.getAttribute('aria-labelledby'); const label = labelled ? (document.getElementById(labelled)?.textContent ?? labelled) : ''; return (label || text(element) || element.getAttribute('title') || element.getAttribute('value') || '').trim().slice(0, 160); }; const element = (node: Element): AccessibilityElement => { const item = node as HTMLElement; const rect = item.getBoundingClientRect(); const style = getComputedStyle(item); return { ref: refs.get(node)!, tagName: item.tagName.toLowerCase(), ...(item.getAttribute('role') ? { role: item.getAttribute('role')! } : {}), ...(item.getAttribute('type') ? { type: item.getAttribute('type')! } : {}), text: text(item), name: name(item), ...(item.id ? { id: item.id } : {}), required: item.hasAttribute('required'), disabled: 'disabled' in item && Boolean((item as HTMLInputElement).disabled), tabIndex: item.tabIndex, visible: style.display !== 'none' && style.visibility !== 'hidden' && rect.width > 0 && rect.height > 0, ...(item.tagName === 'IMG' && item.hasAttribute('alt') ? { alt: item.getAttribute('alt') ?? '' } : {}), ...(item.getAttribute('href') ? { href: item.getAttribute('href')! } : {}), rect: { x: rect.x, y: rect.y, width: rect.width, height: rect.height } }; }; const all = nodes.map(element); const controls = all.filter((item) => ['button', 'a', 'input', 'select', 'textarea'].includes(item.tagName) || item.role); const images = all.filter((item) => item.tagName === 'img'); const ids = new Map<string, string[]>(); Array.from(document.querySelectorAll('[id]')).forEach((node) => { const id = node.id; ids.set(id, [...(ids.get(id) ?? []), refs.get(node) ?? 'dom']); }); return { title: document.title, description: document.querySelector('meta[name="description"]')?.getAttribute('content') ?? '', lang: document.documentElement.getAttribute('lang') ?? '', canonical: Array.from(document.querySelectorAll('link[rel="canonical"]')).map((item) => item.getAttribute('href') ?? '').slice(0, 5), robots: document.querySelector('meta[name="robots"], meta[name="googlebot"]')?.getAttribute('content') ?? '', openGraph: Object.fromEntries(Array.from(document.querySelectorAll('meta[property^="og:"]')).slice(0, 10).map((item) => [item.getAttribute('property')!, item.getAttribute('content') ?? ''])), hreflang: Array.from(document.querySelectorAll('link[rel="alternate"][hreflang]')).slice(0, 20).map((item) => ({ lang: item.getAttribute('hreflang') ?? '', href: item.getAttribute('href') ?? '' })), headings: Array.from(document.querySelectorAll('h1,h2,h3,h4,h5,h6,[role="heading"]')).slice(0, 100).map((item) => ({ ref: refs.get(item) ?? 'heading', level: Number(item.getAttribute('aria-level') ?? item.tagName.substring(1)) || 0, text: text(item) })), duplicateIds: Array.from(ids.entries()).filter(([, values]) => values.length > 1).map(([id, refsForId]) => ({ id, refs: refsForId.slice(0, 10) })).slice(0, 50), landmarks: { main: document.querySelectorAll('main,[role="main"]').length, navigation: document.querySelectorAll('nav,[role="navigation"]').length, banner: document.querySelectorAll('header,[role="banner"]').length, contentinfo: document.querySelectorAll('footer,[role="contentinfo"]').length }, elements: all[0]!, images, controls }; }) as PageMetadataSnapshot;
    const findings = analyzeAccessibilitySeo(snapshot, job.checks);
    for (const finding of findings) { if (await this.scans.isCancellationRequested(job.scanId)) return; await this.issues.upsertFinding({ projectId: job.projectId, detectorId: finding.detectorId, module: 'accessibility-seo', severity: finding.severity, title: finding.title, message: finding.message, fingerprint: finding.detectorId + '|' + job.targetUrl + '|' + (finding.elementRef ?? 'page'), primaryUrl: job.targetUrl, scanId: job.scanId, evidence: { executionId, browser: job.browsers[0] ?? 'chromium', viewport, ...(finding.elementRef ? { elementRef: finding.elementRef } : {}), ...(finding.ruleId ? { ruleId: finding.ruleId } : {}), ...(finding.metadata ?? {}) } }); }
  }
}
