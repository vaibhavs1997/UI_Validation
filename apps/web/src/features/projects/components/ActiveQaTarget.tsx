'use client';
import { useProjects } from '@/features/projects/project-context';
export function ActiveQaTarget() { const { selectedProject, selectedEnvironment } = useProjects(); if (!selectedProject || !selectedEnvironment) return <p className="mt-6 rounded-xl border border-fuchsia-200 bg-white/55 p-4 text-sm text-[#76527f]">Create a project and environment to prepare a QA target.</p>; return <div className="mt-6 grid gap-3 rounded-2xl border border-fuchsia-200/70 bg-white/55 p-5 sm:grid-cols-3"><div><small className="text-xs font-bold uppercase tracking-wider text-[#ad08d1]">Project</small><p className="mt-1 font-semibold text-[#542064]">{selectedProject.name}</p></div><div><small className="text-xs font-bold uppercase tracking-wider text-[#ad08d1]">Environment</small><p className="mt-1 font-semibold text-[#542064]">{selectedEnvironment.name}</p></div><div><small className="text-xs font-bold uppercase tracking-wider text-[#ad08d1]">Target</small><p className="mt-1 break-all text-sm text-[#76527f]">{selectedEnvironment.baseUrl}</p></div></div>; }
