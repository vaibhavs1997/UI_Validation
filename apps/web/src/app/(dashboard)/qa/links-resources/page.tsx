'use client';
import Link from 'next/link';
import { useState } from 'react';
import type { Scan } from '@visionqa/contracts';
import { useProjects } from '@/features/projects/project-context';
import { createScan } from '@/features/scans/scan.service';
import { ScanTargetInput } from '@/features/scans/components/ScanTargetInput';
export default function LinksResourcesPage() { const { selectedProject } = useProjects(); const [url, setUrl] = useState(''); const [selected, setSelected] = useState(['broken-internal-links', 'broken-external-links', 'redirect-quality', 'broken-images']); const [scan, setScan] = useState<Scan | null>(null); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false); const start = async () => { if (!selectedProject || !selected.length) return; setBusy(true); setError(null); try { setScan(await createScan(selectedProject.id, { url, scope: 'single-page', module: 'links-resources', checks: selected, options: { maxExternalLinks: 100 } })); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to start link and resource validation.'); } finally { setBusy(false); } }; return <section className="scan-page"><p className="dashboard-eyebrow">QA CHECK · LINKS</p><h1 className="dashboard-page-title">Links &amp; Resources</h1><p className="dashboard-lead">Validate links and resources from any target URL.</p><div className="scan-config-card"><ScanTargetInput value={url} onChange={setUrl} error={error} /><h2>Checks to run</h2>{['broken-internal-links', 'broken-external-links', 'redirect-quality', 'broken-images'].map((id) => <label className="scan-check" key={id}><input type="checkbox" checked={selected.includes(id)} onChange={(event) => setSelected((current) => event.target.checked ? [...current, id] : current.filter((item) => item !== id))} /> <span>{id.replaceAll('-', ' ')}</span></label>)}<button className="dashboard-primary-button" type="button" disabled={busy || !url.trim() || !selected.length || !selectedProject} onClick={() => void start()}>{busy ? 'Starting…' : 'Run Link &amp; Resource Check'}</button></div>{scan && <div className="scan-confirmation" role="status"><p>{scan.target?.requestedUrl}</p><Link className="dashboard-card-action" href={`/scans/${scan.id}`}>View scan →</Link></div>}</section>; }
