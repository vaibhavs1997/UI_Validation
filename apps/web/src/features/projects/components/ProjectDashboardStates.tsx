import Link from 'next/link';
import type { Project } from '@visionqa/contracts';

const checks = [['Crawl & site structure', '/qa/crawl'], ['Links & resources', '/qa/links-resources'], ['Visual & responsive', '/qa/visual-responsive']] as const;
export function NoProjectDashboard() { return <section><p className="text-xs font-bold tracking-[0.2em] text-[#ad08d1]">GET STARTED</p><h1 className="mt-3 text-3xl font-bold text-[#32133f]">Create your first project</h1><p className="mt-3 max-w-2xl text-[#76527f]">Connect a website to start organizing environments and preparing focused QA checks.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{['Create a project', 'Choose an environment', 'Run your first check'].map((item, index) => <div className="rounded-2xl border border-fuchsia-200/70 bg-white/55 p-5" key={item}><span className="text-sm font-bold text-[#ad08d1]">0{index + 1}</span><h2 className="mt-4 font-semibold text-[#32133f]">{item}</h2></div>)}</div><Link className="liquid-primary mt-8 inline-flex rounded-xl px-6 py-3 font-semibold text-white" href="/projects/new">Create project</Link></section>; }
export function ProjectGettingStartedDashboard({ project }: { project: Project }) { return <section><p className="text-xs font-bold tracking-[0.2em] text-[#ad08d1]">PROJECT OVERVIEW</p><h1 className="mt-3 text-3xl font-bold text-[#32133f]">{project.name}</h1><p className="mt-3 text-[#76527f]">Your project is ready. Add a QA check when you are ready to inspect this environment.</p><div className="mt-8 rounded-2xl border border-fuchsia-200/70 bg-white/55 p-6"><p className="text-sm font-semibold text-[#76527f]">Default environment</p><p className="mt-2 text-xl font-semibold text-[#32133f]">{project.environments[0]?.name ?? 'Production'}</p><p className="mt-1 break-all text-sm text-[#76527f]">{project.environments[0]?.baseUrl ?? project.baseUrl}</p></div><div className="mt-8 grid gap-4 md:grid-cols-3">{checks.map(([label, href]) => <Link className="rounded-2xl border border-fuchsia-200/70 bg-white/55 p-5 transition hover:border-[#ad08d1]" href={href} key={href}><h2 className="font-semibold text-[#32133f]">{label}</h2><p className="mt-2 text-sm text-[#76527f]">Configure this check</p></Link>)}</div></section>; }
export function ProjectHealthDashboard({ project }: { project: Project }) { return <ProjectGettingStartedDashboard project={project} />; }
